name: Cypress Tests

on: [push]

jobs:
  cypress-run:
    runs-on: ubuntu:22.02
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Cypress run
        users: cypress-io/github-action@v2
        with:
          build: npm run build
          start: npm start